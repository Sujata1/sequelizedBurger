<tr class="table-row">
    <td class="table-first-col">{{id}}. {{burger_name}}</td>
    <td><input  class="customer" name="customerName" type=text placeholder="Customer?"></td>
    <td><button class="btn-devourit" data-burgerid={{id}}>Devour it!</button></td>
   
</tr>